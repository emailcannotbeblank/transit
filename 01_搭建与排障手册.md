# CubeSandbox 搭建与排障手册

本文记录本机 `/home/zry/桌面/cubesandbox` 目录下 CubeSandbox 的安装、修复、模板构建、E2B SDK 调用和常见报错处理过程。

当前环境结论：

- 安装目录：`/usr/local/services/cubetoolbox`
- 工作目录：`/home/zry/桌面/cubesandbox`
- CubeAPI 地址：`http://127.0.0.1:3000`
- WebUI 地址：`http://127.0.0.1:12088`
- 沙箱网络：`10.100.0.0/18`
- cube-dev 网关：`10.100.0.1/18`
- 当前可用模板：`tpl-5804e7da7c5d4cc5a0f8fa73`

## 1. CubeSandbox 是什么

CubeSandbox 是腾讯云开源的 AI Sandbox 平台。它不是单独的 QEMU/Firecracker，也不只是类似 E2B 的调度层，而是一套包含控制面、调度、模板制作、网络代理、MicroVM runtime、E2B 兼容 API 和 WebUI 的完整沙箱系统。

可以粗略理解为：

- 对上：提供类似 E2B 的 API，能被 `e2b`、`e2b_code_interpreter` 这类 SDK 调用。
- 中间：有 CubeAPI、CubeMaster、Cubelet、模板中心、调度和状态管理。
- 对下：使用 KVM + 自研/定制 MicroVM runtime，技术路线接近 Cloud Hypervisor / RustVMM / KVM。
- 沙箱内：运行隔离的代码执行环境，例如 `sandbox-code:latest`。
- 网络侧：通过 CubeVS、network-agent、cube-proxy、cube-egress、CoreDNS 处理沙箱 IP、端口暴露、域名和出站代理。

## 2. 本地文件说明

工作目录：

```bash
cd /home/zry/桌面/cubesandbox
```

重要文件：

```text
CubeSandbox/                    # 项目源码
online-install.sh               # 在线安装入口脚本
install-cubesandbox-cn.sh        # 本机封装的 CN 镜像源安装脚本
mount-cubelet-xfs.sh             # 创建 10G XFS loop 文件并挂载到 /data/cubelet
cleanup-cubesandbox.sh           # 清理安装残留，便于失败后重来
reset-cubesandbox-network.sh     # 清理旧 tap 池，修复 CIDR 切换残留
hello.py                         # E2B SDK 调用示例
.env                             # 安装环境变量
```

## 3. 架构与各节点/组件作用

这里的“节点”可以分两层理解：一层是 systemd 服务节点，一层是运行在 Docker 或宿主机上的功能组件。

### 3.1 control target 与 compute target

```text
cube-sandbox-control.target
```

控制面节点集合。单机一键安装时主要启动这个 target，它会拉起控制面、计算面和配套依赖。

```text
cube-sandbox-compute.target
```

计算节点集合。多机部署时，纯计算节点主要运行 Cubelet、network-agent、cube-egress 等组件。

本机当前：

```bash
systemctl list-units 'cube-sandbox-*' --all --no-pager
```

期望看到 `cube-sandbox-control.target` active。

### 3.2 核心 systemd 服务

```text
cube-sandbox-cube-api.service
```

CubeAPI，监听 `3000` 端口，提供 E2B 兼容 API。Python SDK 的 `E2B_API_URL` 指向它：

```bash
export E2B_API_URL="http://127.0.0.1:3000"
```

```text
cube-sandbox-cubemaster.service
```

CubeMaster，控制面核心。负责模板元数据、创建请求、调度、节点状态、模板构建任务等。`cubemastercli` 默认访问它的 `8089` 端口。

```text
cube-sandbox-cubelet.service
```

Cubelet，计算节点核心。负责真正启动沙箱、调用 runtime、创建 MicroVM、挂载 rootfs、处理容器生命周期。

```text
cube-sandbox-network-agent.service
```

网络代理。负责创建和管理 tap 设备、分配沙箱 IP、维护 `cube-dev`、端口映射、CubeVS 相关状态。

```text
cube-sandbox-cube-proxy.service
```

沙箱入口代理，容器名通常是 `cube-proxy`。处理 `*.cube.app` 域名、HTTPS、反向代理到沙箱端口。

```text
cube-sandbox-cube-egress.service
```

透明出站代理，容器名通常是 `cube-egress`。处理沙箱出站访问、MITM CA、审计和访问策略。

```text
cube-sandbox-cube-egress-net.service
```

配置宿主机 TPROXY、iptables/ip rule 等网络规则。通常是 oneshot，状态为 `active exited` 是正常的。

```text
cube-sandbox-coredns.service
```

CoreDNS 容器，为 `cube.app`、`*.cube.app` 等域名提供解析。

```text
cube-sandbox-dns.service
```

宿主机 DNS 路由配置，确保本机访问沙箱域名能走 CubeSandbox 的 DNS。

```text
cube-sandbox-mysql.service
```

MySQL 容器，保存模板、沙箱、节点等控制面元数据。

```text
cube-sandbox-redis.service
```

Redis 容器，作为控制面/网络/代理相关的缓存和消息通道。

```text
cube-sandbox-webui.service
```

WebUI 容器。本机映射端口为 `12088`：

```bash
open http://127.0.0.1:12088
```

或浏览器手动访问。

## 4. 安装前置条件

### 4.1 KVM

检查：

```bash
ls -l /dev/kvm
```

如果没有 `/dev/kvm`，需要在 BIOS/UEFI 开启虚拟化。

### 4.2 内存

一键脚本要求机器内存至少约 8G，实际使用建议更高。

### 4.3 Docker

检查：

```bash
docker ps
```

### 4.4 /data/cubelet 必须在 XFS 上

CubeSandbox 当前使用 cubecow/reflink，要求 `/data/cubelet` 所在文件系统支持 reflink。推荐 XFS。

如果直接在 ext4 根分区上安装，会报：

```text
The filesystem that will host /data/cubelet is on '/' (type: ext4), which is not XFS.
Cube Sandbox requires the /data/cubelet directory to reside on an XFS filesystem.
```

本机采用 10G loop 文件方案。

## 5. 创建 10G XFS 并挂载 /data/cubelet

脚本位置：

```bash
/home/zry/桌面/cubesandbox/mount-cubelet-xfs.sh
```

执行：

```bash
cd /home/zry/桌面/cubesandbox
sudo bash mount-cubelet-xfs.sh
```

脚本大致做了这些事：

```bash
sudo truncate -s 10G /data/cubelet.xfs.img
sudo mkfs.xfs -f /data/cubelet.xfs.img
sudo mkdir -p /data/cubelet
sudo mount -o loop,reflink=1 /data/cubelet.xfs.img /data/cubelet
```

并写入 `/etc/fstab`，让重启后自动挂载。

检查：

```bash
findmnt /data/cubelet
df -Th /data/cubelet
```

期望类型是 `xfs`。

## 6. 安装 CubeSandbox

推荐使用本机封装脚本：

```bash
cd /home/zry/桌面/cubesandbox
sudo bash install-cubesandbox-cn.sh
```

这个脚本主要设置：

```bash
MIRROR=cn
CUBE_SANDBOX_NETWORK_CIDR=10.100.0.0/18
CUBE_SANDBOX_CUBE_EGRESS_IMAGE=cube-sandbox-cn.tencentcloudcr.com/cube-sandbox/cube-egress:v0.4.0
```

为什么不用默认 `192.168.0.0/18`：

本机 Wi-Fi 地址是 `192.168.43.8`，默认沙箱网段 `192.168.0.0/18` 会与宿主网络冲突，所以改为 `10.100.0.0/18`。

安装成功的典型输出：

```text
Pre-download preflight checks passed.
patched cubevs CIDR: 10.100.0.0/18
installed cube-sandbox-control.target
quickcheck OK
```

检查服务：

```bash
systemctl list-units 'cube-sandbox-*' --all --no-pager
docker ps
```

检查端口：

```bash
ss -ltnp | grep -E ':3000|:8089|:12088|:80|:443'
```

## 7. 当前容器说明

查看：

```bash
docker ps --format 'table {{.Names}}\t{{.Image}}\t{{.Status}}\t{{.Ports}}'
```

本机当前主要容器：

```text
cube-webui           WebUI，映射 12088 -> 80
cube-egress          沙箱出站透明代理
cube-proxy           沙箱入口代理，处理 *.cube.app
cube-sandbox-mysql   MySQL 元数据库
cube-sandbox-redis   Redis
cube-proxy-coredns   CoreDNS
```

这些容器会随 systemd target 启动，所以重启机器后自动起来是正常行为。

## 8. 清理和重装

如果安装失败，先用：

```bash
cd /home/zry/桌面/cubesandbox
sudo bash cleanup-cubesandbox.sh
```

默认会清理：

- `cube-sandbox-*` systemd 服务
- CubeSandbox 容器
- `/usr/local/services/cubetoolbox`
- 运行时 socket、临时目录等

默认不会删除：

- `/data/cubelet` XFS 挂载
- Docker volume
- 日志

彻底清理可选：

```bash
sudo bash cleanup-cubesandbox.sh --purge-data --purge-logs
```

如果也要删除 `/data/cubelet` loop 挂载：

```bash
sudo bash cleanup-cubesandbox.sh --remove-cubelet-mount
```

注意：删除挂载会影响已经构建好的模板、镜像层、沙箱状态。

## 9. 修复旧 tap 池/CIDR 残留

曾经遇到的问题：

- Cubelet 配置已经是 `10.100.0.0/18`
- `cube-dev` 已经是 `10.100.0.1/18`
- 但 network-agent 启动后恢复了旧的 `z192.168.*` tap 池
- 创建模板时报 `PortBindingFailed context deadline exceeded`

典型日志：

```text
network-agent tap dequeued from pool: name=z192.168.0.39
network-agent register cubevs tap: sandbox_ip=192.168.0.39
```

修复脚本：

```bash
cd /home/zry/桌面/cubesandbox
sudo bash reset-cubesandbox-network.sh
```

脚本会：

- 停止 CubeSandbox 服务
- 清理 network-agent 状态目录
- 清理 cube-vs object 目录
- 删除旧 `z192.*` tap
- 确认 Cubelet CIDR 是 `10.100.0.0/18`
- 重新启动 CubeSandbox

修复后检查：

```bash
ip -br addr show cube-dev
ip -br link show | awk '$1 ~ /^z[0-9]/ {print $1, $2}' | head
```

期望：

```text
cube-dev       UNKNOWN   10.100.0.1/18 ...
z10.100.0.2    DOWN
z10.100.0.3    DOWN
```

## 10. 构建 sandbox-code 模板

创建模板：

```bash
cubemastercli tpl create-from-image \
  --image cube-sandbox-cn.tencentcloudcr.com/cube-sandbox/sandbox-code:latest \
  --writable-layer-size 1G \
  --expose-port 49999 \
  --expose-port 49983 \
  --probe 49999
```

成功后会返回：

```text
job_id: ...
template_id: ...
status: PENDING
phase: PULLING
```

监听任务：

```bash
cubemastercli tpl watch --job-id <job_id>
```

成功输出：

```text
[7/7] READY progress=100% distribution=1/1 ready, 0 failed
template image job succeeded
```

查看模板：

```bash
cubemastercli tpl list
```

本机当前可用模板：

```text
tpl-5804e7da7c5d4cc5a0f8fa73    READY
```

删除失败模板：

```bash
cubemastercli tpl delete --template-id <failed-template-id>
```

## 11. 模板构建阶段常见问题

### 11.1 skopeo copy TLS handshake timeout

错误：

```text
skopeo copy ... failed
unexpected EOF
net/http: TLS handshake timeout
```

含义：

CubeMaster 在 `BUILDING_EXT4` 阶段用 `skopeo copy docker://...` 拉镜像层，下载腾讯云 TCR/COS blob 时网络中断。

处理：

```bash
cubemastercli tpl delete --template-id <failed-template-id>
```

然后重试 `create-from-image`。

注意：

`docker pull` 不一定解决这个问题，因为 CubeMaster 内部使用 `skopeo copy`，不会直接吃 Docker daemon 的本地缓存。

### 11.2 PortBindingFailed context deadline exceeded

错误：

```text
PortBindingFailed
context deadline exceeded
```

常见原因：

- 旧 `z192.*` tap 池残留
- CubeSandbox CIDR 与宿主网络冲突
- 镜像内部服务没有在 30 秒 probe 窗口内启动

先检查网络：

```bash
ip -br addr show cube-dev
ip -br link show | awk '$1 ~ /^z[0-9]/ {print $1, $2}' | head
```

如果看到 `z192.*`，运行：

```bash
sudo bash /home/zry/桌面/cubesandbox/reset-cubesandbox-network.sh
```

## 12. E2B SDK 调用

### 12.1 安装 Python SDK

在 conda 环境中：

```bash
conda activate pytorch
pip install e2b e2b-code-interpreter
```

### 12.2 环境变量

不要把 `export ...` 当 Python 代码执行。它们应该在 shell 里执行：

```bash
export E2B_API_URL="http://127.0.0.1:3000"
export E2B_API_KEY="e2b_000000"
export CUBE_TEMPLATE_ID="tpl-5804e7da7c5d4cc5a0f8fa73"
export SSL_CERT_FILE="$HOME/.local/share/mkcert/rootCA.pem"
```

其中：

```text
E2B_API_URL       让 E2B SDK 访问本地 CubeAPI
E2B_API_KEY       本地未开鉴权时填任意非空字符串即可
CUBE_TEMPLATE_ID  READY 状态的模板 ID
SSL_CERT_FILE     信任 CubeProxy 的 mkcert 根证书
```

### 12.3 mkcert CA 权限问题

一键安装是用 root 跑的，文档里常见路径是：

```bash
/root/.local/share/mkcert/rootCA.pem
```

普通用户 `zry` 直接读这个文件会报：

```text
PermissionError: [Errno 13] Permission denied
```

处理：

```bash
mkdir -p ~/.local/share/mkcert

sudo cp /root/.local/share/mkcert/rootCA.pem ~/.local/share/mkcert/rootCA.pem
sudo chown zry:zry ~/.local/share/mkcert/rootCA.pem
chmod 644 ~/.local/share/mkcert/rootCA.pem

export SSL_CERT_FILE="$HOME/.local/share/mkcert/rootCA.pem"
```

如果忘记设置 CA，可能报：

```text
certificate verify failed: unable to get local issuer certificate
```

### 12.4 hello.py 示例

推荐写法：

```python
import os
from e2b_code_interpreter import Sandbox

with Sandbox.create(template=os.environ["CUBE_TEMPLATE_ID"]) as sandbox:
    result = sandbox.run_code("""
import os
print("Hello from Cube Sandbox")
print("cpu_count =", os.cpu_count())
""")
    print(result.logs.stdout)
```

运行：

```bash
python hello.py
```

### 12.5 run_code 运行的是 Python，不是 shell

错误写法：

```python
result = sandbox.run_code("nproc")
```

这会被当成 Python 变量名 `nproc`，所以报：

```text
NameError: name 'nproc' is not defined
```

正确写法之一：

```python
result = sandbox.run_code("import os; print(os.cpu_count())")
```

或者用 subprocess：

```python
result = sandbox.run_code("""
import subprocess
print(subprocess.check_output(["nproc"]).decode().strip())
""")
```

## 13. 常用检查命令

### 13.1 服务状态

```bash
systemctl list-units 'cube-sandbox-*' --all --no-pager
```

查看单个服务：

```bash
systemctl status cube-sandbox-cubelet.service --no-pager
systemctl status cube-sandbox-cubemaster.service --no-pager
systemctl status cube-sandbox-network-agent.service --no-pager
```

### 13.2 日志

```bash
journalctl -u cube-sandbox-cubelet.service -n 200 --no-pager
journalctl -u cube-sandbox-cubemaster.service -n 200 --no-pager
journalctl -u cube-sandbox-network-agent.service -n 200 --no-pager
```

文件日志：

```bash
sudo ls /data/log
sudo rg -n "ERROR|WARN|PortBindingFailed|context deadline|skopeo|sandbox_ip" /data/log
```

### 13.3 模板

```bash
cubemastercli tpl list
cubemastercli tpl status --job-id <job-id>
cubemastercli tpl watch --job-id <job-id>
cubemastercli tpl delete --template-id <template-id>
```

### 13.4 网络

```bash
ip -br addr show cube-dev
ip -br link show | awk '$1 ~ /^z[0-9]/ {print $1, $2}' | head
ip route | grep -E '10.100|cube-dev'
```

### 13.5 端口

```bash
ss -ltnp | grep -E ':3000|:8089|:12088|:80|:443|:9090|:19090'
```

### 13.6 容器

```bash
docker ps --format 'table {{.Names}}\t{{.Image}}\t{{.Status}}\t{{.Ports}}'
```

### 13.7 WebUI

```bash
http://127.0.0.1:12088
```

## 14. 端口速查

```text
3000    CubeAPI，E2B 兼容 API
8089    CubeMaster HTTP/API
12088   WebUI 映射端口
3306    MySQL，仅本机 127.0.0.1
6379    Redis，仅本机 127.0.0.1
80/443  cube-proxy，沙箱域名入口
9090    cube-egress admin health
19090   network-agent health/bootstrap
```

## 15. 推荐日常操作顺序

启动/检查：

```bash
systemctl list-units 'cube-sandbox-*' --all --no-pager
docker ps
cubemastercli tpl list
```

如果模板不存在：

```bash
cubemastercli tpl create-from-image \
  --image cube-sandbox-cn.tencentcloudcr.com/cube-sandbox/sandbox-code:latest \
  --writable-layer-size 1G \
  --expose-port 49999 \
  --expose-port 49983 \
  --probe 49999
```

设置 SDK 环境：

```bash
export E2B_API_URL="http://127.0.0.1:3000"
export E2B_API_KEY="e2b_000000"
export CUBE_TEMPLATE_ID="tpl-5804e7da7c5d4cc5a0f8fa73"
export SSL_CERT_FILE="$HOME/.local/share/mkcert/rootCA.pem"
```

运行示例：

```bash
conda activate pytorch
python /home/zry/桌面/cubesandbox/hello.py
```

## 16. 本次排障时间线摘要

1. 第一次安装失败：`/data/cubelet` 在 ext4 上，不满足 XFS/reflink 要求。
2. 创建 10G XFS loop 文件并挂载到 `/data/cubelet`。
3. GitHub 下载失败，改用 `MIRROR=cn`。
4. 默认 `192.168.0.0/18` 与宿主网络冲突，改为 `10.100.0.0/18`。
5. 安装成功后第一次模板失败：旧 `z192.*` tap 池残留，导致 `PortBindingFailed`。
6. 执行 `reset-cubesandbox-network.sh` 清理旧 tap，网络修复为 `z10.100.*`。
7. 第二次模板失败：`skopeo copy` 下载镜像层 TLS timeout，属于网络下载问题。
8. 重试模板构建成功，模板进入 `READY`。
9. Python 示例先遇到 `export` 写进 Python 的语法问题，后遇到 mkcert CA 权限问题。
10. SDK 成功连接后，`run_code("nproc")` 报 `NameError`，原因是 `run_code` 执行 Python 代码，不是 shell 命令。

## 17. 关键提醒

- `/data/cubelet` 必须是 XFS 或其他支持 reflink 的文件系统。
- 宿主机如果在 `192.168.*` 网络中，避免使用默认 `192.168.0.0/18` 沙箱 CIDR。
- 修改 CIDR 后必须清理旧 tap 池，否则 network-agent 可能恢复旧 `z192.*` 设备。
- `create-from-image` 内部用 `skopeo copy` 拉镜像，网络不稳会导致 TLS timeout。
- `E2B_API_URL` 指向 CubeAPI，不是 cube-proxy。
- `SSL_CERT_FILE` 用于信任 `*.cube.app` 的 HTTPS 证书。
- 普通用户不要直接指向 `/root/.local/share/mkcert/rootCA.pem`，应复制到自己的 home。
- `Sandbox.run_code()` 默认执行 Python 代码，不是 shell。

## 18. 停止服务但保留安装

如果只是临时不用 CubeSandbox，不要运行 `cleanup-cubesandbox.sh`。清理脚本会删除部署文件或数据，适合重装排障；日常停用应该只 stop/disable systemd target。

本机当前只有 `cube-sandbox-control.target` 是开机自启入口，其它 `cube-sandbox-*.service` 虽然会运行，但 unit file 状态通常是 disabled，它们是被 control target 拉起的。

### 18.1 停止并关闭开机自启

推荐用脚本：

```bash
cd /home/zry/桌面/cubesandbox
sudo bash stop-cubesandbox.sh
```

等价核心命令：

```bash
sudo systemctl stop cube-sandbox-control.target
sudo systemctl disable cube-sandbox-control.target
sudo systemctl daemon-reload
```

为了确保没有残留服务，也可以补充停止单个服务：

```bash
sudo systemctl stop \
  cube-sandbox-webui.service \
  cube-sandbox-cube-proxy.service \
  cube-sandbox-cube-egress.service \
  cube-sandbox-cube-egress-net.service \
  cube-sandbox-dns.service \
  cube-sandbox-coredns.service \
  cube-sandbox-cube-api.service \
  cube-sandbox-cubemaster.service \
  cube-sandbox-cubelet.service \
  cube-sandbox-network-agent.service \
  cube-sandbox-mysql.service \
  cube-sandbox-redis.service
```

检查是否停止：

```bash
systemctl list-units 'cube-sandbox-*' --all --no-pager
docker ps --format 'table {{.Names}}\t{{.Status}}\t{{.Ports}}'
```

期望：

- `cube-sandbox-control.target` 不再是 active。
- `cube-sandbox-*` 服务不再 running。
- `cube-webui`、`cube-proxy`、`cube-egress`、`cube-sandbox-mysql`、`cube-sandbox-redis`、`cube-proxy-coredns` 等容器停止。

### 18.2 下次恢复服务

推荐用脚本：

```bash
cd /home/zry/桌面/cubesandbox
sudo bash start-cubesandbox.sh
```

等价核心命令：

```bash
sudo systemctl enable cube-sandbox-control.target
sudo systemctl start cube-sandbox-control.target
```

恢复后检查：

```bash
systemctl list-units 'cube-sandbox-*' --all --no-pager
docker ps
cubemastercli tpl list
ip -br addr show cube-dev
```

### 18.3 停止与清理的区别

```text
stop-cubesandbox.sh
```

只停止服务并关闭开机自启，保留：

- systemd unit 文件
- `/usr/local/services/cubetoolbox`
- `/data/cubelet`
- 已构建模板
- MySQL/Redis 数据
- XFS loop 挂载

```text
cleanup-cubesandbox.sh
```

用于安装失败或需要重装时清理环境。它可能删除部署目录、容器、运行时目录；带 `--purge-data` 时还会删除更多数据。日常不用时不要执行清理脚本。
